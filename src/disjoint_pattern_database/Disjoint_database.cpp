#include <queue>          // std::queue

#include "Disjoint_database.hpp"
#include "Board.class.hpp"


const Board sb_663_p1({1, 2, 3 - 1,
                       12, -1, -1, -1,
                       11, 0, -1, -1,
                       10, -1, -1, -1});

const Board sb_663_p2({-1, -1, -1, 4,
                       -1, -1, -1, 5,
                       -1, 0, -1, 6,
                       -1, 9, 8, 7});

const Board sb_663_p3({-1, -1, -1, -1,
                       -1, 13, 14, -1,
                       -1, 0, 15, -1,
                       -1, -1, -1, -1});

std::vector<std::pair<int, int>> make_entries(){
    // std<vector>

    // std::queue <>
};

void DB_663::create()
{
}
